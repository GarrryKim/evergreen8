using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DuckHuntIntro : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("오리를 잡자!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
